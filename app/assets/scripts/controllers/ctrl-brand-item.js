angular.module('productsApp').controller('BrandItemCtrl', [
  '$scope',
  '$rootScope',
  '$location',
  'SearchService',
  '$state',
  '$q',
  '$stateParams',
  '$uibModal',
  '$log',
  function($scope,
           $rootScope,
           $location,
           SearchService,
           $state,
           $q,
           $stateParams,
           $uibModal,
           $log){
    'use strict';

  }
]);
