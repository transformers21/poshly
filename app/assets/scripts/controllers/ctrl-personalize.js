angular.module('productsApp').controller('personalizeCtrl', [
  '$scope',
  'SearchService',
  '$log',
  '$state',
  '$rootScope',
  function($scope,
           SearchService,
           $log,
           $state,
           $rootScope){
    'use strict';
  }
]);
